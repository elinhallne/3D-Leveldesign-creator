using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class StartGeneration : MonoBehaviour
{
    public GameObject TreeGenerationScript, PathGenerationScript;


    void Start()
    {
       // PathGenerationScript.GetComponent<PathCreator>().GeneratePath();
        //Hitta game object med 
        
        //TreeGenerationScript.GetComponent<TreeSpawner>().GenerateTrees();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
